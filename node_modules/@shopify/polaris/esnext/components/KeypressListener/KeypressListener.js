import { useEffect } from 'react';
import { addEventListener, removeEventListener, } from '@shopify/javascript-utilities/events';
export var KeyEvent;
(function (KeyEvent) {
    KeyEvent["KeyDown"] = "keydown";
    KeyEvent["KeyUp"] = "keyup";
})(KeyEvent || (KeyEvent = {}));
export function KeypressListener({ keyCode, handler, keyEvent = KeyEvent.KeyUp, }) {
    const handleKeyEvent = (event) => {
        if (event.keyCode === keyCode) {
            handler(event);
        }
    };
    useEffect(() => {
        addEventListener(document, keyEvent, handleKeyEvent);
        return () => {
            removeEventListener(document, keyEvent, handleKeyEvent);
        };
    });
    return null;
}
